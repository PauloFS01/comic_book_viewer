import { View, Text, StyleSheet } from "react-native";
import React from "react";

export default function ReaderModal({ chieldren }) {
  return (
    <View style={styles.container}>
      <Text>ReaderModal</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {},
});
